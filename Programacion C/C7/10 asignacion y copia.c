#include<stdio.h>
#define MAXLON 10

int main(void)
{
	char original[MAXLON+1] = "cadena";
	char copia[MAXLON+1];
	copia = original;

// para copiar una cadena se debe hacer caracter a caracter
return 0;
}

