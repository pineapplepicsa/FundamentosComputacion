/*	En este ejemplo vemos el formato comentado y un ejemplo simple: (que no compila)

tipo_de_retorno identificador (parametros)
{
	cuerpo de la funcion
}

Ejemplo:*/

float suma (float x, float b)
{
	float resultado;
	resultado = x * b;
	return(resultado);
}
