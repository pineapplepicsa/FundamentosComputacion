//OPERADORES


//OPERADORES RELACIONALES

// a == b	Igual a
// a != b	No igual a
// a >  b	Mayor que
// a <  b	Menor que
// a >= b	Mayor o igual que
// a <= b	Menor o igual que


//OPERADORES LOGICOS

// !(x >= y)			No logica					Negacion (!)
// m < n && i > j       operando-1 && operando-2	Y logica (&&)
// m = 5 || n != 10		operando-1 || operando-2	O logica ||


// OPERADOR (PARENTESIS)

// Es el operador de llamada a funciones. Sirve para encerrar los argumentos de una función, efectuar
// conversiones explicitas de tipo, indicar en el seno de una declaracion que un identificador corresponde
// a una funcion, resolver los conflictos de prioridad entre operadires.


// OPERADOR [CORCHETE]

// Sirve para dimensionar los arrays y designar un elemento de un array.



